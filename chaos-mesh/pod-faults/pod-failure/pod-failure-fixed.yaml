apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata:
  name: pod-failure-fixed-springboot
  namespace: default
spec:
  action: pod-failure
  mode: fixed
  duration: '1m'
  value: '2'
  selector:
    labelSelectors:
      'app': 'springboot-starterkit'