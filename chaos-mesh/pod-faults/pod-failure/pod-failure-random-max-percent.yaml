apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata:
  name: pod-failure-random-max-percent-springboot
  namespace: default
spec:
  action: pod-failure
  mode: random-max-percent
  duration: '30s'
  value: '80'
  selector:
    labelSelectors:
      'app': 'springboot-starterkit'