apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata:
  name: pod-failure-all-springboot
  namespace: default
spec:
  action: pod-failure
  mode: all
  duration: '1m'
  selector:
    labelSelectors:
      'app': 'springboot-starterkit'